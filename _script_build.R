# Note that you could use blogdown::build_site(), then publish the public/ folder using Netlify drag & drop
blogdown::build_site()

# Start the Hugo server using blogdown::serve_site() (only once due to the magic of LiveReload)
blogdown::serve_site()
